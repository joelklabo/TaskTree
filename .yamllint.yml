extends: default
ignore: |
  .venv/
  backend/.venv/
  **/.venv/**
rules:
  line-length:
    max: 120
  truthy: disable
  comments: disable
  braces:
    max-spaces-inside: 1
    min-spaces-inside: 0
  document-start: disable
  trailing-spaces:
    level: warning
